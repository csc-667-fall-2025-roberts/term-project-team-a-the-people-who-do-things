<!DOCTYPE html>
<html lang="en" class="h-full bg-gray-50">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Results - Scrabble</title>
    
    <% if (NODE_ENV === 'production') { %>
        <link rel="stylesheet" href="/styles/main.css">
    <% } %>
</head>
<body class="h-full flex flex-col bg-gray-50">
<%- include('../components/header') %>

<main class="flex-1 overflow-y-auto w-full px-4 py-10">
    
    <div class="mx-auto max-w-[800px] bg-white p-8 rounded-lg shadow-lg text-center">
        
        <h1 class="text-4xl font-bold text-blue-600 mb-2">Game Over!</h1>
        <p class="text-slate-500 mb-10">The match has ended. Here are the final standings.</p>

        <section class="mb-10 text-left">
            <h2 class="text-xl font-bold text-gray-900 mb-4 pb-2 border-b border-gray-200">Final Scores</h2>
            
            <div id="scores-list" class="flex flex-col gap-3">
                <div class="p-4 text-center text-slate-400 italic">Loading scores...</div>
            </div>
        </section>

        <section class="mb-10 text-left">
            <h2 class="text-xl font-bold text-gray-900 mb-4 pb-2 border-b border-gray-200">Game Statistics</h2>
            
            <div id="stats-container" class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                <div class="col-span-full p-4 text-center text-slate-400 italic">Loading statistics...</div>
            </div>
        </section>

        <div class="flex flex-col sm:flex-row justify-center gap-4 mt-8">
            <a href="/lobby" class="inline-flex justify-center items-center px-6 py-3 rounded-md bg-blue-600 font-semibold text-white shadow-sm hover:bg-blue-500 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Return to Lobby
            </a>
            
            <button id="rematch-btn" class="inline-flex justify-center items-center px-6 py-3 rounded-md bg-white border border-gray-300 font-semibold text-gray-700 shadow-sm hover:bg-gray-50 transition focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
                Request Rematch
            </button>
        </div>
      </div>
    </main>
    
    <% if (NODE_ENV === 'production') { %>
        <script type="module" src="/js/main.js"></script>
    <% } else { %>
        <script type="module" src="http://localhost:5173/ts/main.ts"></script>
    <% } %>
  </body>
</html>
